// Public API of the feature. Import from '@/features/health' only — never from internal paths.
export { HealthStatus } from './components/HealthStatus'
export { healthKeys, healthQueryOptions, type Health } from './api/health.api'
